services:
  linux-learning:
    build:
      context: .
      dockerfile: Dockerfile.3rd
    container_name: linux_study_server
    restart: always
    volumes:
      - ./workspace:/workspace
    ports:
      - "8888:8888"
      - "2222:22"
